#include <iostream>
using namespace std;

int main() {
    int x = 10; // x is the referent.
    int &ref = x; // ref is the  reference to x. We must always initialise a reference with a variable to which it is
    // referring to.
    // a reference always need an initializer.
    return 0;
}
